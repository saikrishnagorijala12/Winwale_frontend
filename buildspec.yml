version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies
      - npm install

  build:
    commands:
      - echo Building Vite app
      - npm run build

artifacts:
  files:
    - '**/*'
  discard-paths: no
